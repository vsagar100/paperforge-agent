# Copy this file to .env and provide only the key value (without the word Bearer).
OLLAMA_API_KEY=

# Optional. The default is Ollama Cloud. Use localhost only when intentionally
# running Ollama on this computer.
# OLLAMA_HOST=http://localhost:11434
